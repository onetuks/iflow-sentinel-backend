# AGENT.md (Backend)

> 이 파일은 Claude Code와 Gemini CLI 양쪽에서 공용으로 사용하는 에이전트 지침서입니다.
> 프로젝트 루트에 `AGENT.md`로 두고, 아래처럼 심볼릭 링크(또는 복사본)를 만들어두면
> 두 도구가 각자의 관례 파일명으로도 동일한 내용을 읽습니다.
>
> ```bash
> ln -s AGENT.md CLAUDE.md
> ln -s AGENT.md GEMINI.md
> ```

## 프로젝트 개요

Java / Spring Boot 기반 백엔드 프로젝트입니다.
설계서(요구사항 정의서, 아키텍처 문서 등)가 별도로 존재하며, 에이전트는 이를 최우선 근거로 삼아
구현합니다. 다만 설계서에 빠진 부분이나 애매한 부분이 있으면 임의로 확대 해석하지 말고,
**사용자(세영님)의 피드백을 받아 보완**하는 방식으로 진행합니다.

## 핵심 규칙

### 1. Controller / Service / Repository 계층 분리

- **Controller**: HTTP 요청/응답 처리만 담당. 요청 파라미터 검증(또는 `@Valid` 위임), DTO 변환,
  적절한 Service 메서드 호출, 응답 반환. 비즈니스 로직을 절대 포함하지 않습니다.
- **Service**: 비즈니스 로직 담당. 트랜잭션 경계(`@Transactional`)는 원칙적으로 Service 계층에 둡니다.
  여러 Repository를 조합하거나, 도메인 규칙을 검증하는 로직이 여기에 위치합니다.
- **Repository**: 데이터 접근만 담당 (JPA Repository 인터페이스 또는 QueryDSL 등). 비즈니스 로직을
  포함하지 않습니다.
- 계층 간 호출 방향은 항상 `Controller → Service → Repository` 한 방향으로 유지하고, 역방향 참조나
  계층 건너뛰기(Controller가 Repository 직접 호출 등)를 하지 않습니다.
- DTO(요청/응답)와 Entity는 분리합니다. Controller/Service 경계에서 DTO ↔ Entity 변환을 명시적으로 합니다.

```
com.inspien.xxx
 ├─ controller/
 │   └─ TodoController.java
 ├─ service/
 │   └─ TodoService.java
 ├─ repository/
 │   └─ TodoRepository.java
 ├─ domain/ (또는 entity/)
 │   └─ Todo.java
 └─ dto/
     ├─ TodoRequest.java
     └─ TodoResponse.java
```

### 2. SOLID 원칙 준수 및 응집화

- **SRP (단일 책임)**: 클래스/메서드가 하나의 책임만 갖도록 합니다. 하나의 Service가 여러 도메인의
  비즈니스 로직을 뒤섞어 갖지 않도록 주의합니다.
- **OCP (개방-폐쇄)**: 규칙/전략이 추가될 가능성이 있는 부분(예: 검증 규칙, 파서 로직)은 인터페이스로
  추상화해서 확장에 열려있도록 설계합니다. 단, 과도한 추상화(YAGNI 위반)는 지양 — 실제로 확장 요구가
  있는 지점에만 적용합니다.
- **LSP / ISP**: 인터페이스는 클라이언트(호출부)가 필요로 하는 만큼만 작게 나눕니다. 구현체가 인터페이스
  계약을 어기지 않도록 합니다.
- **DIP (의존 역전)**: Service는 Repository의 구체 구현이 아니라 인터페이스에 의존합니다. 외부 시스템
  연동(예: SAP OData 호출 등)이 필요한 경우, 이를 인터페이스로 추상화한 뒤 Service에서는 인터페이스만
  바라보게 합니다.
- **응집화**: 관련된 상수, 검증 로직, 매핑 로직은 흩어놓지 않고 관련 도메인/클래스 안에 모아둡니다.
  (예: `Todo` 관련 상태 전이 규칙은 `TodoService`나 `Todo` 도메인 객체 안에 모아두고, 다른 곳에
  중복 작성하지 않습니다.)

### 3. 설계서 반영 및 피드백 루프

- 구현 전에는 항상 설계서(요구사항 정의서/아키텍처 문서 등)의 관련 부분을 먼저 확인하고, 이를 충실히
  반영하는 것을 우선합니다.
- 설계서와 다르게 구현하거나, 설계서에 없는 내용을 임의로 추가해야 할 경우에는:
  1. 왜 그렇게 판단했는지 먼저 설명하고,
  2. 사용자에게 확인/피드백을 요청한 뒤,
  3. 승인된 내용만 반영합니다.
- 설계서 자체의 허점이나 개선 여지가 보이면, 임의로 고치지 않고 "이 부분은 이렇게 보완하면 좋을 것 같다"는
  형태로 제안만 하고 최종 결정은 사용자에게 맡깁니다.

## 코드 스타일/컨벤션

- 패키지 구조는 계층형(controller/service/repository/domain/dto)을 기본으로 하되, 설계서에
  다른 구조가 명시되어 있으면 그것을 우선합니다.
- 예외 처리는 커스텀 예외 + `@ControllerAdvice`(전역 예외 핸들러)로 일관되게 처리합니다.
- 트랜잭션, 검증, 로깅 등 횡단 관심사는 특정 계층(Service 또는 별도 AOP)에 일관되게 위치시키고
  여러 계층에 중복 구현하지 않습니다.

## 작업 시 체크리스트 (에이전트 자가 점검용)

- [ ] Controller에 비즈니스 로직이 섞여 있지 않은가?
- [ ] Service가 Repository 구체 구현이 아닌 인터페이스에 의존하는가?
- [ ] 하나의 클래스/메서드가 여러 책임을 동시에 지고 있지 않은가?
- [ ] 관련 로직/상수가 여러 곳에 중복되어 흩어져 있지 않은가?
- [ ] 설계서 내용과 실제 구현이 일치하는가? 다르다면 사용자에게 확인을 구했는가?

## 참고 (프론트엔드와의 연계)

프론트엔드는 현재 `db.ts` 더미 데이터 + API 레이어 방식으로 먼저 개발되고 있으며, 추후 이 백엔드
API로 교체될 예정입니다. 따라서 응답 DTO의 필드명/구조를 설계할 때, 프론트엔드에서 사용하기 쉬운
형태(불필요하게 중첩되지 않은 구조 등)를 함께 고려하면 좋습니다.
